[runner]
nanvixd_binary_path = "./bin/nanvixd.elf"
working_directory = "."
log_directory = "logs"
tmp_directory = "/tmp"
ipv4_addr = "127.0.0.1"
port_num = 9999
fatal = true
toolchain_path = "toolchain"
sysroot_path = "./sysroot"
nanvixd_shutdown_attempts_max = 10
nanvixd_shutdown_retry_interval_ms = 100
nanvixd_ready_attempts_max = 50
nanvixd_ready_retry_interval_ms = 100
cleanup_uservm_sleep_duration_ms = 10
stream_collection_timeout_ms = 300000
gateway_connect_max_attempts = 100
gateway_connect_initial_backoff_ms = 10
gateway_connect_max_backoff_ms = 500
gateway_connect_timeout_ms = 15000

[[tests]]
build_modes = ["debug", "release"]
executor = "empty"
name = "empty/1"
iterations = 1

[[tests]]
build_modes = ["debug", "release"]
executor = "empty"
name = "empty/2"
iterations = 2

#===================================================================================================
# HTTP Executor Tests (Non-Interactive Mode)
#===================================================================================================

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-kernel.elf"
extra_nanvixd_args = "-ramfs ./bin/test-kernel-ramfs.img"
expect_empty_output = true
expected_exit_code = 13

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-mmio-fault.elf"
extra_nanvixd_args = "-ramfs ./bin/test-kernel-ramfs.img"
expect_empty_output = true
expected_exit_code = 4

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-sigsegv.elf"
extra_nanvixd_args = "-ramfs ./bin/test-kernel-ramfs.img"
expect_empty_output = true
expected_exit_code = 0

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-vfs-test.elf"
extra_nanvixd_args = "-ramfs ./bin/test-rust-vfs-test.img"
expect_empty_output = true
expected_exit_code = 0

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/echo-rust-nostd.initrd"
input = '["hello world!"]'
expected_output = "hello world!"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-thread.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-file.initrd"
extra_nanvixd_args = "-ramfs ./bin/standalone-rootfs.img"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-misc.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["release"]
executor = "http"
program = "./bin/test-rust-memory.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-dlfcn.initrd"
extra_nanvixd_args = "-ramfs ./bin/standalone-rootfs.img"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-fork-kcall.initrd"
expect_empty_output = true
expected_exit_code = 13

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-fork-guestfs.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-fork-guestfs.img"
expect_empty_output = true
expected_exit_code = 13

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-waitpid.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-kill.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-job-control.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-setenv.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["release"]
executor = "http"
program = "./bin/test-rust-stress.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-arch.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-linux-app.initrd"
extra_nanvixd_args = "-ramfs ./bin/standalone-rootfs.img"
expected_output = "ok"

#===================================================================================================
# Terminal Executor Tests (Interactive Mode)
#===================================================================================================

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-kernel.elf"
extra_nanvixd_args = "-ramfs ./bin/test-kernel-ramfs.img"
expect_empty_output = true
expected_exit_code = 13

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-mmio-fault.elf"
extra_nanvixd_args = "-ramfs ./bin/test-kernel-ramfs.img"
expect_empty_output = true
expected_exit_code = 4

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-sigsegv.elf"
extra_nanvixd_args = "-ramfs ./bin/test-kernel-ramfs.img"
expect_empty_output = true
expected_exit_code = 0

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-vfs-test.elf"
extra_nanvixd_args = "-ramfs ./bin/test-rust-vfs-test.img"
expect_empty_output = true
expected_exit_code = 0

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/echo-rust-nostd.initrd"
input = "hello world"
expected_output = "hello world"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-thread.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-file.initrd"
extra_nanvixd_args = "-ramfs ./bin/standalone-rootfs.img"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-misc.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["release"]
executor = "terminal"
program = "./bin/test-rust-memory.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-dlfcn.initrd"
extra_nanvixd_args = "-ramfs ./bin/standalone-rootfs.img"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-fork-kcall.initrd"
expect_empty_output = true
expected_exit_code = 13

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-fork-guestfs.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-fork-guestfs.img"
expect_empty_output = true
expected_exit_code = 13

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-waitpid.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-kill.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-job-control.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-setenv.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["release"]
executor = "terminal"
program = "./bin/test-rust-stress.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-arch.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
name = "execv-small"
program = "./bin/test-rust-execv-test.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-execv-test.img"
expected_output = "ok"
expected_exit_code = 0

# execv() of a large binary (target inflated to MEMORY_SIZE/8). Reuses the same caller, pointed at
# a ramfs whose `/target` is the large program, to exercise loading a big image with no size cap.
[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
name = "execv-big"
program = "./bin/test-rust-execv-test.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-execv-big-test.img"
expected_output = "ok"
expected_exit_code = 0

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-linux-app.initrd"
extra_nanvixd_args = "-ramfs ./bin/standalone-rootfs.img"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-cmdline-len.elf"
# 4092 (MAX_CMDLINE_ARGS_LEN) - 25 (filename) - 1 (space separator) = 4066.
program_args_padding_len = 4066
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-cmdline-len.elf"
# 4092 (MAX_CMDLINE_ARGS_LEN) - 25 (filename) - 1 (space separator) = 4066.
program_args_padding_len = 4066
expected_output = "ok"


#===================================================================================================
# Host Mount Tests
#===================================================================================================

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-mount-test.initrd"
extra_nanvixd_args = "-mount ./bin/mount-test-data"
expected_output = "ok"
expected_exit_code = 0
runs_on = ["microvm"]

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-fork-hostfs.initrd"
extra_nanvixd_args = "-mount ./bin/test-fork-hostfs-data"
expect_empty_output = true
expected_exit_code = 13
runs_on = ["microvm"]

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-mount-multipart-test.initrd"
expected_output = "ok"
expected_exit_code = 0
runs_on = ["microvm"]

#===================================================================================================
# Networking Tests
#===================================================================================================

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-network.initrd"
extra_nanvixd_args = "-allow-host-networking"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-network.initrd"
extra_nanvixd_args = "-allow-host-networking"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-pipe-dup2.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-fork-exec-vfsd-test.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-fork-exec-vfsd-test.img"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-fork-exec-write-test.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-fork-exec-write-test.img"
expected_output = "ok"

# Acceptance test for file-descriptor inheritance across fork()+execv(): a
# descriptor opened before fork() must remain usable by the child after it
# execv()s. FAILS while the exec'd image does not receive the parent's cloned
# descriptor table (the inherited fd is unknown and the child's read fails).
# Release-only: loops fork()+execv() many times to stress the path, which is too
# slow to run under a debug/trace build within the per-test timeout.
[[tests]]
build_modes = ["release"]
executor = "http"
program = "./bin/test-rust-fork-exec-loop-test.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-fork-exec-loop-test.img"
expected_output = "ok"

# Acceptance test for execv() arguments containing spaces: an argument with an
# embedded space must reach the new image verbatim as a single argument. FAILS
# while execv() flattens argv/envp into a space-separated string (a token with a
# space is rejected with EINVAL, or would be split). Size-independent. This is
# why fork()+execv()'d CPython cannot run python -c "<code with spaces>".
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-fork-exec-argv-space-test.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-fork-exec-argv-space-test.img"
expected_output = "ok"

# Acceptance test for bulk pipe data integrity across fork()+execv(): a large
# payload streamed by an exec'd child through a dup2()'d pipe must reach the
# parent in full. FAILS while a bulk transfer from a LARGE exec'd image does not
# reach the parent (the writer's write() fails part-way / the reader sees a short
# stream), worsening per iteration; a small exec'd image does not trigger it.
# Release-only: the target is a deliberately large image streamed many times, which is
# prohibitively slow under a debug (trace-enabled) build but tractable under release.
[[tests]]
build_modes = ["release"]
executor = "http"
program = "./bin/test-rust-fork-exec-pipe-bulk-test.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-fork-exec-pipe-bulk-test.img"
expected_output = "ok"

# Acceptance test for repeated pipe-capture reliability across fork()+execv(): a
# moderate payload streamed by an exec'd child through a dup2()'d pipe must reach
# the parent in full on EVERY cycle, across many cycles, for a large exec'd image.
# Gated to release builds: it loads a large image many times, which is tractable
# only when tracing is disabled (a debug/trace build emits per-page kernel trace
# over a byte-at-a-time UART and would exceed the per-test timeout).
[[tests]]
executor = "http"
program = "./bin/test-rust-fork-exec-pipe-loop-test.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-fork-exec-pipe-loop-test.img"
expected_output = "ok"
build_modes = ["release"]

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-fork-exec-write-test.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-fork-exec-write-test.img"
expected_output = "ok"

# Acceptance test for multi-threaded VFS per-process state attribution (nanvix/nanvix#2529).
# A secondary (non-main) thread performs read/write on descriptors the main thread opened. FAILS
# while vfsd derives the caller PID by casting the request's TID: the non-main thread's distinct
# TID maps to a phantom process whose descriptor table is empty, so the I/O returns EBADF.
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-thread-vfs-test.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-thread-vfs-test.img"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-thread-vfs-test.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-thread-vfs-test.img"
expected_output = "ok"

# Release-only: loops fork()+execv() many times to stress the path, which is too
# slow to run under a debug/trace build within the per-test timeout.
[[tests]]
build_modes = ["release"]
executor = "terminal"
program = "./bin/test-rust-fork-exec-loop-test.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-fork-exec-loop-test.img"
expected_output = "ok"

# Acceptance test for socket reference-counting across fork() (nanvix/nanvix#2609).
# FAILS until sockets are reference-counted across fork(): a child closing its
# inherited copy currently destroys the parent's socket.
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-socket-fork.initrd"
extra_nanvixd_args = "-allow-host-networking"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-pipe-dup2.initrd"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-fork-exec-vfsd-test.initrd"
extra_nanvixd_args = "-ramfs ./bin/test-rust-fork-exec-vfsd-test.img"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-socket-fork.initrd"
extra_nanvixd_args = "-allow-host-networking"
expected_output = "ok"

#===================================================================================================
# Environment Variable Tests
#===================================================================================================

# Single environment variable.
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-env-nostd.elf"
program_env = "TEST_VAR=hello"
expected_output = "TEST_VAR=hello\nok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-env-nostd.elf"
program_env = "TEST_VAR=hello"
expected_output = "TEST_VAR=hello\nok"

# Multiple environment variables.
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-env-nostd.elf"
program_env = "FOO=bar BAZ=qux"
expected_output = "FOO=bar\nBAZ=qux\nok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-env-nostd.elf"
program_env = "FOO=bar BAZ=qux"
expected_output = "FOO=bar\nBAZ=qux\nok"

# Environment variable with special characters in value.
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-env-nostd.elf"
program_env = "SPECIAL=a/b:c-d_e.f"
expected_output = "SPECIAL=a/b:c-d_e.f\nok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-env-nostd.elf"
program_env = "SPECIAL=a/b:c-d_e.f"
expected_output = "SPECIAL=a/b:c-d_e.f\nok"

#===================================================================================================
# Combined Command-Line Argument and Environment Variable Tests
#===================================================================================================

# Program with args and environment variables.
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-cmdline-env-nostd.elf"
program_args = "hello world"
program_env = "FOO=bar"
expected_output = "ARGS:hello world\nENV:FOO=bar\nok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-cmdline-env-nostd.elf"
program_args = "hello world"
program_env = "FOO=bar"
expected_output = "ARGS:hello world\nENV:FOO=bar\nok"

# Program with args and no environment variables.
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-cmdline-env-nostd.elf"
program_args = "hello world"
expected_output = "ARGS:hello world\nENV:\nok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-cmdline-env-nostd.elf"
program_args = "hello world"
expected_output = "ARGS:hello world\nENV:\nok"

# Program with no args and with environment variables.
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-cmdline-env-nostd.elf"
program_env = "FOO=bar"
expected_output = "ARGS:\nENV:FOO=bar\nok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-cmdline-env-nostd.elf"
program_env = "FOO=bar"
expected_output = "ARGS:\nENV:FOO=bar\nok"

# Program with escaped args (literal semicolon in arguments).
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-cmdline-env-nostd.elf"
program_args = "a;b"
program_env = "X=1"
expected_output = "ARGS:a;b\nENV:X=1\nok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-cmdline-env-nostd.elf"
program_args = "a;b"
program_env = "X=1"
expected_output = "ARGS:a;b\nENV:X=1\nok"

# Program with escaped environment variables (literal semicolon in env value).
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-cmdline-env-nostd.elf"
program_args = "hello"
program_env = "PATH=a;b"
expected_output = "ARGS:hello\nENV:PATH=a;b\nok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-cmdline-env-nostd.elf"
program_args = "hello"
program_env = "PATH=a;b"
expected_output = "ARGS:hello\nENV:PATH=a;b\nok"

#===================================================================================================
# getenv() Environment Lookup Tests
#===================================================================================================

# getenv() returns kernel-cmdline-provided variables (including HOME); a key that was never
# provided resolves to null.
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-getenv-nostd.elf"
program_env = "HOME=/scratch NVX_GETENV_A=alpha NVX_GETENV_B=beta"
expected_output = "GETENV:HOME=/scratch NVX_GETENV_A=alpha NVX_GETENV_B=beta NVX_GETENV_MISSING=(null)\nok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-getenv-nostd.elf"
program_env = "HOME=/scratch NVX_GETENV_A=alpha NVX_GETENV_B=beta"
expected_output = "GETENV:HOME=/scratch NVX_GETENV_A=alpha NVX_GETENV_B=beta NVX_GETENV_MISSING=(null)\nok"

# A malformed entry (a bare KEY token with no '=') is skipped, so getenv() returns null for it.
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-getenv-nostd.elf"
program_env = "HOME=/data NVX_GETENV_A=alpha NVX_GETENV_B"
expected_output = "GETENV:HOME=/data NVX_GETENV_A=alpha NVX_GETENV_B=(null) NVX_GETENV_MISSING=(null)\nok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-getenv-nostd.elf"
program_env = "HOME=/data NVX_GETENV_A=alpha NVX_GETENV_B"
expected_output = "GETENV:HOME=/data NVX_GETENV_A=alpha NVX_GETENV_B=(null) NVX_GETENV_MISSING=(null)\nok"

# An empty environment produces an empty table, so every probed key resolves to null.
[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-rust-getenv-nostd.elf"
expected_output = "GETENV:HOME=(null) NVX_GETENV_A=(null) NVX_GETENV_B=(null) NVX_GETENV_MISSING=(null)\nok"

[[tests]]
build_modes = ["debug", "release"]
executor = "terminal"
program = "./bin/test-rust-getenv-nostd.elf"
expected_output = "GETENV:HOME=(null) NVX_GETENV_A=(null) NVX_GETENV_B=(null) NVX_GETENV_MISSING=(null)\nok"

#===================================================================================================
# Snapshot Save / Restore Tests
#===================================================================================================

[[tests]]
build_modes = ["debug", "release"]
executor = "snapshot-restore"
program = "./bin/test-rust-snapshot-test.elf"
iterations = 1
expected_exit_code = 32

# Asserts that the host nanvixd process exits on its own after the snapshot artifacts are
# written to disk. The guest workload spins forever after taking the snapshot, so the only
# path to a clean nanvixd exit is the host-side snapshot-completion tear-down.
[[tests]]
build_modes = ["debug", "release"]
executor = "snapshot-save-exit"
program = "./bin/test-rust-snapshot-test.elf"
iterations = 1
expected_exit_code = 0
