[runner]
nanvixd_binary_path = "./bin/nanvixd.elf"
working_directory = "."
log_directory = "logs"
tmp_directory = "/tmp"
ipv4_addr = "127.0.0.1"
port_num = 9999
hwloc_file_path = ""
l2_enabled = true
fatal = true
toolchain_path = "toolchain"
sysroot_path = "./sysroot"
nanvixd_shutdown_attempts_max = 60
nanvixd_shutdown_retry_interval_ms = 300
nanvixd_ready_attempts_max = 50
nanvixd_ready_retry_interval_ms = 100
netns_pool_size = 0
cleanup_uservm_sleep_duration_ms = 10
cleanup_l2_uservm_sleep_duration_ms = 500
stream_collection_timeout_ms = 300000
tcp_cleanup_max_wait_seconds = 70
tcp_cleanup_poll_interval_seconds = 2
gateway_connect_max_attempts = 100
gateway_connect_initial_backoff_ms = 10
gateway_connect_max_backoff_ms = 500
gateway_connect_timeout_ms = 15000

[[tests]]
build_modes = ["debug", "release"]
executor = "empty"
name = "empty/1"
iterations = 1

[[tests]]
build_modes = ["debug", "release"]
executor = "empty"
name = "empty/2"
iterations = 2

#===================================================================================================
# HTTP Executor Tests (Non-Interactive Mode)
#===================================================================================================

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-kernel.elf"
extra_nanvixd_args = "-ramfs ./bin/test-kernel-ramfs.img"
input = "[]"
expect_empty_output = true
expected_exit_code = 13

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/test-mmio-fault.elf"
extra_nanvixd_args = "-ramfs ./bin/test-kernel-ramfs.img"
input = "[]"
expect_empty_output = true
expected_exit_code = 3
runs_on = ["microvm"]

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/echo-rust-nostd.elf"
input = '["hello world!"]'
expected_output = "hello world!"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/linux-app.elf"
input = "[]"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/arch-rust.elf"
input = "[]"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/memory-rust.elf"
input = "[]"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/misc-rust.elf"
input = "[]"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/network-rust.elf"
input = "[]"
expected_output = "ok"

[[tests]]
build_modes = ["debug", "release"]
executor = "http"
program = "./bin/thread-rust.elf"
input = "[]"
expected_output = "ok"
