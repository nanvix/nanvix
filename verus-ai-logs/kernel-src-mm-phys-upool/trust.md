# Trust Boundaries: upool

No external-bottom trust boundaries exist in the upool module.

All functions in `upool.rs` are fully body-verified with no `external_body`,
`assume_specification`, or `axiom` items.
