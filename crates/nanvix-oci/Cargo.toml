[package]
name = "nanvix-oci"
description = "OCI annotation constants and parsing for Nanvix container images"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
oci-spec = { workspace = true }
anyhow = { workspace = true }
log = { workspace = true }

[dev-dependencies]
tempfile = "3"
